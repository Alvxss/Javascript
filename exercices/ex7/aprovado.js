export function media(n1, n2, n3) {
  let soma = n1 + n2 + n3;

  let resultado = soma / 3;
  
  return resultado;
}

export function aprovado(media) {
  let resultado = media >= 5;

  return resultado;
}